1. 放置的时候应该优先取镜头中心射线到当前载具的交点作为优先选择落点
2. 节点应该需要可以使用移动工具进行移动，同时连带其附属的梁和面板变化
3. 现在的网格对齐使用的是网格线交点，实际应该用格子本身，现在造成在边缘位置梁无法对齐
4. 给轴向吸附增加一个快捷键


现在的github page build workflow因为涉及到编译，从而需要花钱，不能正确完成
能否改为预build然后静态托管的形式

1. 现在的方块库下面的section高度实在太窄了，导致根本显示不出来方块，需要能够大概充满left-sidebar
2. left-sidebar的可拓展最大宽度调大一些，同时方块卡片在hover上时应该需要能够直接显示id全称
3. 增加一个设置选项，开关长度显示，打开之后可以在所有梁上显示xyz长度，方便玩家进行参考
4. 在放置物体或者梁的时候，应该直接产生一个虚影，方便玩家进行对齐


1. 现在的梁交点边缘仍然不能完全覆盖，梁的起始和结束点应该是覆盖满对应的开始结束格的
2. 面板在创建的时候不应该是节点来创建，应该是选择梁来创建，参考原本Anymaker游戏的逻辑，同时面板创建之后应该存在一个法向的offset，而不是在梁正中间的一个膜
3. 参考原本游戏的上色逻辑，增加涂色工具

---

增强导入功能，参考test-vehicle的数据和游戏截图，以及游戏的各种逻辑，需要基本一致的导入进来，以导入后渲染图像和test-vehicle\vehicle.png对比为验收

1. 完善当前的梁工具，增加切分、在已有梁上新增点的功能
2. 完善游戏原本的接线工具、皮带工具等，逻辑全部需要还原

---

1. 在镜头拉远到一定距离后，不再渲染参考网格和节点
2. 增加光照设定，包括光照角度、强度、阴影强度和可调整的背景颜色
3. 增加可开关的镜头正交渲染
4. 现在在梁/面/模型渲染的时候，经常会因为重叠而产生渲染上的虚色区域，修复这类问题


1. 在选择涂色工具之后，色板在section top-tool-section下方新加一个悬浮工具条，可以点击颜色进行调整，同时可以存储多个快捷颜色
2. 增加光照柔和度的调整
3. 把选中方块属性和资源与校验放在编辑器参数的上方
4. 现在的连接工具有问题，需要跟游戏保持完全一样，反编译游戏来看一下连接怎么做的，包括不同方块的输入输出节点位置需要在使用连接工具的时候展示出来，连接工具也需要类似涂色工具，在下方选择不同类型的连接
5. 在导入测试vehicle的时候，不同子网格好像没有正确的挂在车上，比如车门
6. 使用删除或选择工具的时候，在编辑器中高亮对应的部件

---

Anymaker游戏路径：I:\SteamLibrary\steamapps\common\Anymaker\game.exe
参考一下Stormworks Analysis里的反编译能力：D:\00Data00\Stormworks\analysis
把游戏路径和反编译能力也加载在当前仓库中

1. 在右侧增加历史tab，显示过去50次操作历史，可回溯/恢复
2. 在下方不显示当前有多少节点、梁、面板，取而代之显示当前载具的总长宽高信息，格子数量和实际长度


根据反编译内容，继续确认需要完成TODO中P6的所有内容：保证游戏载具数据的每一分毫都完整的导入进了编辑器，不要有任何信息损失，然后导出的时候也完完整整。同时所有内容都是和游戏完全一致可修改的

在方块库中默认屏蔽家具和建材两类型物品，可手动打开

不同类型的连接端点应该使用不同颜色，对应连接工具子菜单中也应该有不同颜色
电线黄色、机械橙色、液体蓝色、气体绿色、皮带灰色、数据紫色


---

增加一个透明化工具，点击之后可以使选中组件/面/梁不可见，使玩家可以编辑内部结构，场景中存在不可见物品时，在toolbar右侧显示一个“取消透明化”按钮，恢复原样